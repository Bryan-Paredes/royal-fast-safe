---
import { TextGenerateEffect } from "@/components/ui/text-generate-effect";
import { InteractiveButton } from "@/components/ui/interactive-button";
import { flags, type FlagImage } from "@/data/hero";
---

<header class="relative flex h-dvh w-full items-center justify-center">
  <main
    class="bg-[url('/hero.webp')] relative h-full w-full bg-cover bg-center pb-28 drop-shadow-lg"
  >
    <div
      class="flex flex-col h-full w-full items-center justify-center p-4 gap-11 mx-auto my-auto max-w-4xl"
    >
      <div class="flex flex-col items-center justify-center gap-10 text-center">
        <div class="sm:mb-8 flex justify-center">
          <div class="flex gap-6">
            {
              flags.map((flag: FlagImage) => (
                <img
                  src={flag.src}
                  alt={flag.alt}
                  class="h-9 w-9"
                  decoding="async"
                  loading="eager"
                />
              ))
            }
          </div>
        </div>
        <h1 class="mb-8">
          <TextGenerateEffect
            client:load
            words="Your Partner for Complex Logistics"
          />
        </h1>
        <!-- <p class="text-xl text-left max-w-xl text-gray-300">
          We specialize in freight, heavy haul, and container shipping across
          the USA, Mexico, and Canada. Our expert team is dedicated to
          delivering reliable and efficient logistics services tailored to your
          unique needs.
        </p> -->
      </div>
      <div class="flex items-center justify-center gap-5">
        <a href="/quote" class="uppercase dark:text-white">
          <InteractiveButton> Free Quote Now </InteractiveButton>
        </a>
      </div>
    </div>
  </main>
</header>
